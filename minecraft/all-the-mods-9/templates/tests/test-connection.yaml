apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "all-the-mods-9.fullname" . }}-test-connection"
  labels:
    {{- include "all-the-mods-9.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "all-the-mods-9.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
