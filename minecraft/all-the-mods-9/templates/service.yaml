apiVersion: v1
kind: Service
metadata:
  name: {{ include "all-the-mods-9.fullname" . }}
  labels:
    {{- include "all-the-mods-9.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "all-the-mods-9.selectorLabels" . | nindent 4 }}
